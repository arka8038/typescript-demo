// Tuples - Array with some order of elements and fixed length

type Drink = [string, boolean, number]; // Tuple type for drink

const pepsi: [string, boolean, number] = ['pepsi', true, 40]; // Tuple with string, boolean, and number types
const coke: Drink = ['coke', false, 30]; // Another tuple with the same structure
